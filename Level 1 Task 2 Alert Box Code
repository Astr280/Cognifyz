Level 1 Task 2 Alert Box Code 

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Greeting Based on Time</title>
    <script>
        function displayGreeting() {
            const now = new Date();
            const hours = now.getHours();
            let greeting;

            if (hours < 12) {
                greeting = "Good Morning!";
            } else if (hours < 18) {
                greeting = "Good Afternoon!";
            } else {
                greeting = "Good Evening!";
            }

            alert(greeting);
        }

        // Call the function when the window loads
        window.onload = displayGreeting;
    </script>
</head>
<body>
    <h1>Welcome to the Greeting Page</h1>
    <p>The greeting will be displayed in an alert box based on the current time.</p>
</body>
</html>
